# Architecture

High-level architecture of this project. Keep it current — it's the map AI agents and
new humans use to find their way around.

## Overview

<one paragraph: what the system does, who uses it, what value it delivers>

## Components

### Backend services

| Service | Purpose | Tech | Port |
|---------|---------|------|------|
| <name>  | <what>  | .NET 10 | <port> |

### Frontend

- Angular SPA at `frontend/`
- Hosted at `<URL in production>`

### Infrastructure

- **PostgreSQL** — primary datastore. One database per service.
- **RabbitMQ** — async messaging between services.
- **Identity provider** — <Keycloak | IdentityServer>, issues JWTs.

## Data flow

<describe how data moves through the system; reference services and queues by name>

## Deployment

- **Target**: VPS via Docker Compose
- **CI/CD**: GitHub Actions on push to main → build → push image → SSH deploy
- **Migrations**: run automatically before app starts (see deploy workflow)

## See also

- ADRs in `docs/adr/` for architectural decisions and their context
- `.ai/project-context.md` for AI-facing context
- `.ai-kit/ai/core.md` for s3lab standards this project follows
