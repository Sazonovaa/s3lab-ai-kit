# s3lab-policy v0.1.0
[extend]
useDefault = true

[allowlist]
description = "Per-repo overrides — add paths or regexes here to silence false positives. Commit this file so the team agrees on what is allowlisted."
# gitleaks >=8.30 requires allowlist to declare at least one of commits/paths/regexes/stopwords.
# Placeholder stopword never appears in real code; replace with real entries when needed.
stopwords = ["__s3lab_policy_placeholder_never_match__"]
paths = []
regexes = []
